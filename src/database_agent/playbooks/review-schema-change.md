# Playbook: Review Schema Change

## Focus

- migration exists
- migration order is safe
- defaults and nullability are compatible
- old and new app versions can coexist if rollout is staged
- indexes match query paths
- delete/cascade semantics are explicit
